#include <iostream>
using namespace std;

int main() {
    //获取参数
    int n, m, a[100] = { 0 }, temp;
    cin >> n >> m;

    //获取数列
    for (int i = 0; i < n; i++) 
        cin >> a[i];

    //开始交换
    for (int i = 0; i < m; i++) {

    }

    return 0;
}